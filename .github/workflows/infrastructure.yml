name: Deploy Infrastructure

on:
  push:
    branches: [main]
    paths:
      - "infrastructure/**"
      - "services/**"

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Build & push video batch image
        run: |
          ECR_URI=$(aws ecr describe-repositories \
            --repository-names video-thumbnail-processor \
            --region ${{ secrets.AWS_REGION }} \
            --query 'repositories[0].repositoryUri' \
            --output text)
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} \
            | docker login --username AWS --password-stdin $ECR_URI
          cd services/batch/video-thumbnail-processor
          docker build -t $ECR_URI:${{ github.sha }} -t $ECR_URI:latest .
          docker push $ECR_URI:${{ github.sha }}
          docker push $ECR_URI:latest

      - name: Install & Deploy
        env:
          CLOUDFRONT_PUBLIC_KEY_PEM: ${{ secrets.CLOUDFRONT_PUBLIC_KEY_PEM }}
          CLOUDFRONT_PRIVATE_KEY_SECRET_ARN: ${{ secrets.CLOUDFRONT_PRIVATE_KEY_SECRET_ARN }}
        run: |
          cd services && npm ci
          cd ../infrastructure && npm ci
          npx cdk deploy psilo-dev-apse1-stack --require-approval never -c imageTag=${{ github.sha }}
